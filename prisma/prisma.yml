

endpoint: http://${env:PRISMA_API_ENDPOINT}:4466/${env:APP_NAME}/dev
datamodel: datamodel.graphql
generate:
  - generator: graphql-schema
    output: ../src/generated/prisma.graphql
hooks:
  post-deploy:
    - prisma generate